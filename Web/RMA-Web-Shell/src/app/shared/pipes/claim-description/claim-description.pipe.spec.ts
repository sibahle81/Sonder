import { ClaimDescriptionPipe } from './claim-description.pipe';

describe('ClaimDescriptionPipe', () => {
  it('create an instance', () => {
    const pipe = new ClaimDescriptionPipe();
    expect(pipe).toBeTruthy();
  });
});
