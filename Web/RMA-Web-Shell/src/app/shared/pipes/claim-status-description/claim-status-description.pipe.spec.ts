import { ClaimStatusDescriptionPipe } from './claim-status-description.pipe';

describe('ClaimStatusDescriptionPipe', () => {
  it('create an instance', () => {
    const pipe = new ClaimStatusDescriptionPipe();
    expect(pipe).toBeTruthy();
  });
});
