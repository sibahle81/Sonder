import { ClaimReasonPipe } from './claim-reason.pipe';

describe('ClaimReasonPipe', () => {
  it('create an instance', () => {
    const pipe = new ClaimReasonPipe();
    expect(pipe).toBeTruthy();
  });
});
