export enum SourceSystemEnum {
    CompCare = 1,
    Modernisation = 2,
}

export enum SourceModuleEnum
{
    ClientCare = 1,
    ClaimCare = 2,
    PensCare = 3,
    MediCare = 4,
    FinCare = 5,
}

export enum SourceProcessEnum {
    Inception = 1,
    Maintenance = 2,
    Cancellation = 3,
    ReInstate = 4,
    OverPayment = 5,
    BundleRaise = 6,
    ManuallyAuthorised = 7,
}
