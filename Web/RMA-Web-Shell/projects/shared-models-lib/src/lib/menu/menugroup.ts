import { MenuItem } from './menuitem';

// wiki: http://bit.ly/2B2EQcO
// The menu group is the header of the accordion in the left menu.

/** @description The menu group is the header of the accordion in the left menu. */
export class MenuGroup {
    title: string;
    menuItems: MenuItem[];
}
