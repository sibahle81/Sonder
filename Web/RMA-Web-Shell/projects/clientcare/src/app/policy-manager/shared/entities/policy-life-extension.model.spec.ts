import { PolicyLifeExtension } from './policy-life-extension.model';

describe('PolicyLifeExtension', () => {
  it('should create an instance', () => {
    expect(new PolicyLifeExtension()).toBeTruthy();
  });
});
