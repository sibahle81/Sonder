CREATE TABLE [common].[DebtCollectionTransactionStatus] (
    [Id]   INT          NOT NULL,
    [Name] VARCHAR (50) NOT NULL,
    CONSTRAINT [PK_DebtCollectionTransactionStatus] PRIMARY KEY CLUSTERED ([Id] ASC)
);

