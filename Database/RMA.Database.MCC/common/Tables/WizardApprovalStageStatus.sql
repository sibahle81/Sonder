CREATE TABLE [common].[WizardApprovalStageStatus] (
    [Id]   INT          NOT NULL,
    [Name] VARCHAR (50) NOT NULL,
    CONSTRAINT [PK_WizardApprovalStageStatus] PRIMARY KEY CLUSTERED ([Id] ASC)
);

