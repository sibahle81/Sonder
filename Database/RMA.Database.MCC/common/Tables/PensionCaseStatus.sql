CREATE TABLE [common].[PensionCaseStatus] (
    [Id]   INT          IDENTITY (1, 1) NOT NULL,
    [Name] VARCHAR (50) NOT NULL,
    CONSTRAINT [PK__commom_PensionCaseStatus] PRIMARY KEY CLUSTERED ([Id] ASC)
);

