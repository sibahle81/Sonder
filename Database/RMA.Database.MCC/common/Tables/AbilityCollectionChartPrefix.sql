CREATE TABLE [common].[AbilityCollectionChartPrefix] (
    [Id]   INT          NOT NULL,
    [Name] VARCHAR (50) NOT NULL,
    CONSTRAINT [PK_CollectionChartPrefix] PRIMARY KEY CLUSTERED ([Id] ASC)
);

