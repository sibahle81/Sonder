CREATE TABLE [common].[DeclarationRenewalStatus] (
    [Id]   INT          IDENTITY (1, 1) NOT NULL,
    [Name] VARCHAR (50) NOT NULL,
    CONSTRAINT [PK_DeclarationRenewalStatus] PRIMARY KEY CLUSTERED ([Id] ASC)
);

